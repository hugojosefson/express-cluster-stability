{
  "name": "express-cluster-stability",
  "version": "1.4.1",
  "description": "Stability and multi-core performance for your Express app, via the built-in stable Node.js cluster API.",
  "main": "lib/index.js",
  "scripts": {
    "start": "npm run compile && npm run compile-examples && npm install express && node --harmony examples-es5/full-example",
    "prepublish": "npm run compile",
    "compile": "rm -rf lib; babel src -d lib",
    "compile-examples": "rm -rf examples-es5; babel examples -d examples-es5",
    "test": "npm run compile && npm run compile-examples"
  },
  "keywords": [
    "cluster",
    "express",
    "stability",
    "stable",
    "uptime",
    "master",
    "worker",
    "node",
    "processes"
  ],
  "author": "Hugo Josefson <hugo@josefson.org>",
  "repository": "hugojosefson/express-cluster-stability",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-plugin-add-module-exports": "^0.1.2",
    "babel-plugin-array-includes": "^2.0.3",
    "babel-preset-es2015": "^6.5.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ],
    "plugins": [
      "add-module-exports",
      "array-includes"
    ]
  },
  "engines": {
    "node": ">=4"
  },
  "license": "MIT"
}
